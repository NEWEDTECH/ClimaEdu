rules_version = '2';

// Craft rules based on your security requirements
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all users for development
    // In production, you should restrict access based on authentication and authorization
    match /{allPaths=**} {
      allow read, write: if true;
    }
    
    // Example of a more restrictive rule for user uploads
    // match /users/{userId}/{allPaths=**} {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && request.auth.uid == userId;
    // }
    
    // Example of a more restrictive rule for content uploads
    // match /contents/{contentId}/{allPaths=**} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
  }
}
